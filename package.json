{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "PORT=19761 BASE_PATH=/ pnpm --filter @workspace/website run dev",
    "dev:api": "PORT=8080 pnpm --filter @workspace/api run dev",
    "dev:mock": "PORT=19761 BASE_PATH=/ pnpm --filter @workspace/website run dev & PORT=8080 pnpm --filter @workspace/api run mock; wait",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./apps/**\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
